@using IAT2022.ViewModels
@model AdminViewModel
<h1>Konfiguration</h1>

<form>
    <div class="searchDiv">
        <input id="search" placeholder="Sök på projektnamn" name="search" type="text"/>
        <input asp-controller="Admin" asp-action="SearchProjects" type="submit" value="Sök"/>
    </div>
</form>

<div class="dropdown">
  <button class="dropbtn">Välj vilken kategori du vill uppdatera</button>
 
  <div class="dropdown-content">
    <a onclick="showCustomer()">Behov</a>
    <a onclick="showProduct()">Lösning</a>
    <a onclick="showIPR()">IPR</a>
    <a onclick="showTeam()">Team</a>
    <a onclick="showBusiness()">Marknad</a>
    <a onclick="showFinance()">Finansiering</a>
  </div>
</div>
<div> <button onclick="closeAllCategories()"><i class="fa-solid fa-rectangle-xmark">Rensa</i></button></div>
<div class="adminContent">
<div id="CustomerQuestionChanger">
    @if (Model.CustomerQuestions!=null)
    {
        @foreach(var q in Model.CustomerQuestions)
        {
            <a asp-action="SelectCustomerQuestion" asp-controller="Admin" asp-route-id="@q.Id"><div class="singleQuestion">
                <p>@q.Id.ToString()</p>
                <p>@q.QuestionDescription</p>
            </div></a>
        }
    }
    
</div>
<div id="ProductQuestionChanger">
    @if (Model.ProductQuestions!=null)
    {
        @foreach(var q in Model.ProductQuestions)
        {
            <a asp-action="SelectProductQuestion" asp-controller="Admin" asp-route-id="@q.Id"><div class="singleQuestion">
                <p>@q.Id.ToString()</p>
                <p>@q.QuestionDescription</p>
            </div></a>
        }
    }
    
</div>
<div id="IPRQuestionChanger">
    @if (Model.IprQuestions!=null)
    {
        @foreach(var q in Model.IprQuestions)
        {
            <a asp-action="SelectIprQuestion" asp-controller="Admin" asp-route-id="@q.Id"><div class="singleQuestion">
                <p>@q.Id.ToString()</p>
                <p>@q.QuestionDescription</p>
            </div></a>
        }
    }
    
</div>
<div id="TeamQuestionChanger">
    @if (Model.TeamQuestions!=null)
    {
        @foreach(var q in Model.TeamQuestions)
        {
            <a asp-action="SelectTeamQuestion" asp-controller="Admin" asp-route-id="@q.Id"><div class="singleQuestion">
                <p>@q.Id.ToString()</p>
                <p>@q.QuestionDescription</p>
            </div></a>
        }
    }
    
</div>
<div id="BusinessQuestionChanger">
    @if (Model.BusinessQuestions!=null)
    {
        @foreach(var q in Model.BusinessQuestions)
        {
            <a asp-action="SelectBusinessQuestion" asp-controller="Admin" asp-route-id="@q.Id"><div class="singleQuestion">
                <p>@q.Id.ToString()</p>
                <p>@q.QuestionDescription</p>
            </div></a>
        }
    }
    
</div>
<div id="FinanceQuestionChanger">
    @if (Model.FinanceQuestions!=null)
    {
        @foreach(var q in Model.FinanceQuestions)
        {
            <a asp-action="SelectFinanceQuestion" asp-controller="Admin" asp-route-id="@q.Id"><div class="singleQuestion">
                <p>@q.Id.ToString()</p>
                <p>@q.QuestionDescription</p>
            </div></a>
        }
    }
    
</div>
</div>